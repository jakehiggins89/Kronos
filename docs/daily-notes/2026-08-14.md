# Daily Kronos adjustment - 2026-08-14

## Verdict

NO-GO / `blocked`. Keep live trading, live alerts, and paper promotion disabled.

Friday produced the first canonical index, validation, and candidate set after the source-faithful Potter detector correction. The corrected detector restored useful signal coverage and produced two scan-level promotions, but neither is authorized for paper or live action: the bullish validation cohort is still negative, the global score still does not rank outcomes after costs, and all current equity rows use research-grade IEX data. The increased yield is useful evidence, not a reason to bypass the downstream gates.

## Scheduled operating state

- The 13:30 CT `research_ops` cycle completed in 536.39 seconds with evidence run ID `20260814T183436Z-c1729579`.
- The 14:30 recovery launch returned `skipped_already_complete` and retained the verified NYSE window (`13:30Z` open, `20:00Z` close).
- Task Scheduler reports `0x00000000`, zero missed runs, and the next launch Monday, 2026-08-17 at 13:30 CT.
- Journal integrity was 1,243 input / 1,243 unique / zero duplicates.
- Outcome review checked five pending counterfactuals and resolved none.

## Accepted, rejected, and resolved trade review

The research scan accepted one new counterfactual: `T` bullish, research score 68, Doctrine v2 score 75/pass, and Kronos agreement 0%/fail. It is pending outcome tracking only. The journal now has 71 resolved research candidates (31 wins / 40 losses raw) and six pending; the adaptive policy held threshold 65 with `hold_no_edge` and applied no override.

The corrected Edge scan had 20 skips, eight rejects, two scan-level promotions, zero paper-authorized candidates, and zero live trades:

- `T` bullish scored 72.45. Its seven cost-adjusted analogs averaged +0.9954R with a 71.4% win rate; the Potter setup and fresh Tradier OPRA contract passed. It remains research-only because bullish validation is negative and the equity feed is IEX (`feed_confidence=0.7`).
- `AFRM` bullish scored 67.40. Its seven net analogs averaged +0.4658R with an 85.7% win rate; Potter and Tradier OPRA passed. It is also research-only for the same direction and feed reasons. Its research-scan score was only 53, which is why it did not enter the separate threshold-65 counterfactual cohort.
- `IONQ` bullish was the strongest reject at 38.13 with -0.1513R net analog expectancy; it stayed below the Edge research threshold and failed positive expectancy.
- `GME` bearish scored 16.09 with +0.6295R analog expectancy but failed the source setup gate and the Edge research threshold. `MARA` bearish showed the same structural failure at 5.57 / +0.3264R.
- The other five active rejects scored 1.79 or less. `KEY` also had no liquid Tradier contract. No reject merits an override.

The 55-ticker retrieval index now contains 11,085 records. Cross-source checks remain green; the run correctly excluded every current incomplete daily bar and the known CLSK placeholder row.

## Go-live evidence

Readiness remains blocked on `validation_threshold_55_unsupported` and `ranking_evidence_unsupported`:

- all 1,500 validation rows have complete positive risk denominators and are net of the immutable 25 bps/side cost floor;
- threshold 55 now has 15 raw signals across 13 evidence days, versus 20 required, but its day-level precision is 50.0% with a 29.25% lower bound, average -0.0959R, and HAC t=-0.4878;
- pooled net rank IC is +0.0112 with entry-day-clustered p=0.373610, far below the required +0.07 / p<=0.05 evidence;
- the top decile spans 150 raw rows / 57 evidence days but averages -0.1600R per entry day, with HAC t=-1.9711 and dependence-aware precision lower bound 0.2741;
- bullish validation averages -0.0847R across 65 days (t=-1.1275), while bearish averages -0.1952R across 64 days (t=-2.0025). Both directions remain blocked.

Compared with Thursday's old-detector evidence, threshold-55 coverage improved from one to 13 evidence days and rank IC moved from -0.0209 to +0.0112. Economics did not pass: both directions and the top decile remain negative. This is exactly why detector fidelity and readiness authorization must remain separate.

## Operator-message safety fix

Today's Telegram status brief contradicted its own blocked audit. It printed `Live alerts: off` and then labelled `T` and `AFRM` as `LIVE TRADES - 2` because it counted every Edge `research`/`promote` recommendation as an executed trade. A scan-level recommendation is not an execution, and the audit explicitly listed zero execution-ready promotions.

`scanner/brief.py` now always keeps Edge recommendations separate from live trades. A blocked or research-only recommendation renders under `EDGE RESEARCH` with a paper-authorization count. Only a `promote` row named by the audit's `execution_ready_promoted_candidates` and accompanied by `readiness=paper_trade_only` may render as a `PAPER CANDIDATE`; it is still never called a live trade. Regression coverage proves both the blocked and paper-authorized paths.

The corrected current rendering is:

- `LIVE TRADES - none`
- `EDGE RESEARCH - 2`
- `30 scanned, 2 Edge recommendations, 0 paper-authorized`

The incorrect message had already been sent before this automation pass. I did not resend Telegram or rerun the market scan after hours. The fix applies to the next scheduled brief while today's original research evidence remains intact.

## Verification

- Red regression: the old brief reproduced `LIVE TRADES - 2` for a blocked audit.
- Focused brief suite: 23 passed.
- Full suite: 353 passed with the two known NumPy all-NaN calibration warnings.
- Runtime report/tuning hash inventory: 1,373 files before and after pytest, zero created, removed, or changed.
- `compileall`: passed.
- `pip check`: no broken requirements.
- Doctor: `status=ok`.
- `git diff --check`: passed with CRLF normalization notices only.

## Next run

Keep live trading, live alerts, and paper promotion disabled. Monday should collect the next independent corrected-detector sample without loosening any score, expectancy, direction, cost, feed, options, or statistical gate. Watch whether threshold-55 coverage grows with positive net economics; current coverage growth alone is not readiness.

Current run time: 2026-08-14T18:07:14-05:00.
