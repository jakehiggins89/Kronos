# Daily Kronos adjustment - 2026-07-27

## Verdict

Not live-ready. Live trading and live alerts remain disabled.

The hardened scheduled task completed its Monday market-hours cycle successfully (`0x00000000`) from `2026-07-27T18:30:03Z` through `18:36:41Z` in 398.25 seconds. Journal integrity was clean at 853 input / 853 unique records with no duplicates removed.

## Accepted and rejected evidence

- Outcome review checked 11 pending rows and resolved one counterfactual. The July 17 `F` bullish research candidate (score 73, Doctrine v2 60) finished the exact five-session horizon as a very small win: +0.1039% / +0.0354R. It experienced -2.8233% MAE and +2.4747% MFE before the horizon exit.
- The research journal now has 47 resolved candidates (25 wins / 22 losses) and 11 pending. The 11-day same-ticker embargo leaves 27 independent rows (13 wins / 14 losses).
- Today's Potter research scan logged one new counterfactual: `CHPT` bearish score 68, Doctrine v2 score 60, Kronos directional agreement 0.40. It is research-only evidence, not a trade or alert.
- The Edge scan had 8 active rejects and 22 skips, with no Edge research candidates or promotions. Top rejects were `F` bullish 41.78, `T` bullish 30.51, and `PFE` bullish 9.39.
- All 8 active rows failed the setup gate and remained below the Edge research threshold. `GME`, `HOOD`, and `LUNR` also had non-positive analog expectancy. `KEY` had no contract that cleared the option-liquidity gates.
- Execution-grade Tradier/OPRA options evidence was available for the otherwise active candidates; the sole option warning was the genuinely untradeable `KEY` chain. Equity feed confidence remains research-grade IEX.

Readiness still fails both independent evidence routes:

- `validation_threshold_55_unsupported`: 0 supporting signals versus 20 required.
- `ranking_evidence_unsupported`: pooled IC +0.0077 (p 0.3828), bullish IC +0.0354, bearish IC -0.0380, and top-decile average R +0.0074 with t 0.0650 and Wilson lower-bound precision 0.3557.

## Fixed: marginal direction means could unlock promotion

The readiness audit described a direction as “proven positive” and placed it in `promotable_directions` whenever it had at least 15 samples and average R merely above zero. Today's bullish validation cohort exposed the weakness: 863 samples averaged only +0.0156R, while the day-clustered t-stat was 1.3758. That is not enough evidence to distinguish the mean from noise, but the audit labeled bullish promotable.

This did not change today's blocked verdict because both global evidence routes fail and there were no promoted candidates. It was still a pre-live fail-open: a future global gate pass could have admitted a promoted candidate from a marginally positive direction.

Direction promotion now requires all three:

- At least 15 validation samples.
- Positive average R.
- Day-clustered R-multiple t-stat at least 2.0.

Negative directions remain blocked. Under-sampled, zero-mean, missing-stat, and weak-stat directions are now explicitly `unproven_directions` and cannot produce `paper_trade_only` readiness. The daily brief renders that state. On today's regenerated audit, bearish is `BLOCKED`, bullish is `UNPROVEN`, and `promotable_directions` is empty.

## Verification

- Red regression before the fix: a promoted bullish row with +0.20 average R but day-clustered t 1.38 produced `paper_trade_only`.
- Focused audit/ranking/brief slice: 37 passed.
- Full suite: 311 passed with the same two expected all-NaN calibration warnings.
- `compileall`: passed.
- `pip check`: no broken requirements.
- `scanner.main --mode doctor`: `status=ok`.
- `git diff --check`: passed with CRLF normalization notices only.
- Audit and local brief were regenerated without sending a duplicate Telegram message.

## Next action

Keep live disabled and continue market-hours `research_ops`. Do not promote either direction until a global evidence route passes and the candidate's own direction also has statistically supported positive expectancy.

Current run time: `2026-07-27T18:09:16-05:00`.
