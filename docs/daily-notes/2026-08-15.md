# Daily Kronos adjustment - 2026-08-15

## Verdict

NO-GO / `blocked`. Keep live trading, live alerts, and paper promotion disabled.

Today is Saturday. I preserved Friday's canonical market-hours scan, validation, journal, and policy artifacts instead of manufacturing a duplicate closed-market sample. Task Scheduler remains healthy (`0x00000000`, zero missed runs) and the next scheduled launch is Monday, 2026-08-17 at 13:30 CT.

## Friday evidence review

Friday remains one accepted research-only counterfactual (`T` bullish, research score 68, Doctrine v2 75/pass, Kronos 0%/fail), two scan-level Edge recommendations (`T` and `AFRM` bullish), eight active rejects, 20 skips, zero paper-authorized candidates, and zero live trades. All active equity rows use research-grade IEX data and both validation directions lose money after costs. No rejected row merits an override.

The saved audit remains blocked on `validation_threshold_55_unsupported` and `ranking_evidence_unsupported`. Its canonical figures are 13 threshold-55 evidence days at -0.0959R/day, rank IC +0.0112 with clustered p=0.373610, and a top decile at -0.1600R/day with t=-1.9711. Those files were not regenerated off-session.

## Validated-score scale bug

I found a score comparability bug exposed by the detector-corrected Friday scan. Historical retrieval rows cannot reconstruct point-in-time equity-feed confidence or option-chain quality, but those execution-only fields changed the numeric Edge score:

- a full-SIP live row could gain four feed-quality points over an otherwise identical historical row (Friday's IEX rows gained two);
- an historical row with no option-chain snapshot automatically lost eight options-quality points, while a current Tradier OPRA row lost none;
- the same strategy setup and analog outcomes therefore scored 77.76 with healthy live execution fields versus 65.76 with historical defaults in the red regression.

Threshold validation and live candidate scoring were not using one scale. This could create a scan-level promotion whose numeric score was never represented by the evidence claimed to support that threshold.

`scanner/edge/scoring.py` now limits the numeric Edge score to historically reconstructable setup and outcome evidence. Data quality, feed confidence, option availability, spread, and options provenance remain fail-closed promotion/execution gates and still emit their existing blocking reasons, but they no longer manufacture or subtract Edge points. The execution-related scorecard keys remain at zero for report compatibility.

I also fixed a related reporting omission: `_score_edge_for_bars()` now carries `gross_analog_summary` and `analog_cost_model` into each scan candidate instead of dropping them after scoring. Monday's report will expose the gross/net comparison, 25 bps/side cost basis, and risk-denominator coverage directly.

## Read-only replay impact

I replayed Friday's saved index and candidate features in memory under the corrected scorer. No report, journal, tuning, policy, alert, or Telegram artifact was written.

- `T` moves from 72.45 to 70.45 and `AFRM` from 67.40 to 65.40. Both remain scan-level recommendations but stay research-only because bullish validation is negative and IEX is not capital-grade.
- All eight rejects remain rejects. `IONQ` moves 38.13 -> 36.13, `GME` 16.09 -> 14.09, `MARA` 5.57 -> 3.57, and `LUNR` 1.79 -> 0.00; the other four remain at zero.
- Historical threshold-55 coverage rises from 15 raw signals / 13 evidence days to 56 raw signals / 41 evidence days because the artificial eight-point historical options penalty is gone.
- The added coverage is decisively negative: threshold 55 averages -0.2760R per entry day, HAC t=-2.9396, with dependence-aware precision lower bound 0.2052.
- Rank IC remains unsupported at +0.0196 with clustered p=0.282282. The top decile remains -0.1600R/day, t=-1.9711, precision lower bound 0.2741; bullish and bearish direction economics remain negative.

The score-scale correction therefore strengthens the NO-GO verdict. Coverage is no longer the practical threshold-55 problem; negative net expectancy and absent ranking skill are.

## Verification

- Red regression failed before the fix with 77.76 versus 65.76 for identical strategy evidence.
- Focused scoring test: 15 passed.
- Focused Edge/brief slice: 76 passed.
- Full suite: 354 passed with the two known NumPy all-NaN calibration warnings.
- Runtime report/tuning inventory: 1,373 files, aggregate SHA-256 unchanged before and after tests.
- `compileall`: passed.
- `pip check`: no broken requirements.
- Doctor: `status=ok`.
- `git diff --check`: passed with CRLF normalization notices only.
- Targeted tracked-diff secret-assignment scan: no matches.

## Next run

Keep all live and paper paths disabled. Monday should regenerate the first canonical report on the historically comparable score scale and expose per-candidate gross/net analog provenance. Do not loosen thresholds: verify that threshold-55 remains negative with the larger honest cohort, and continue accumulating independent detector-corrected evidence.

Current run time: 2026-08-15T18:08:41-05:00.
