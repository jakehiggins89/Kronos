# Daily Kronos adjustment - 2026-08-01

## Verdict

NO-GO / `blocked`. Live trading, live alerts, and paper promotion remain disabled.

Today is Saturday, so I preserved Friday's execution-grade market-hours snapshot instead of creating duplicate research samples from the same source session or replacing Tradier quotes with closed-market evidence. The scheduled task is healthy and will collect the next independent snapshot Monday, 2026-08-03 at 13:30 CT.

## Accepted and resolved research evidence

There were no accepted live trades and no Edge promotions on Friday.

- The sole newly accepted research sample was `RIVN` bearish at research score 65. Doctrine v2 scored 48 / fail and Kronos agreement was 0.30 / fail. It remains a pending counterfactual and cannot alert or trade.
- Outcome review resolved three July 23 bearish counterfactuals, all losses: `LYFT` stopped at -11.3661% / -1R, `CHPT` exited at the horizon at -1.0009% / -0.1339R, and `PLTR` stopped at -0.7590% / -1R.
- The journal has 53 resolved research candidates (27 wins / 26 losses raw) and 14 pending. The adaptive policy still holds `RESEARCH_CANDIDATE_MIN_SCORE=65`; no strategy or tuning threshold changed.

## Rejected trades audited

Friday's canonical Edge scan had 25 skips, five active rejects, zero Edge research candidates, and zero promotions:

- `PLTR` bearish, Edge 35.31: positive seven-analog cohort (+0.4712R, 85.7% raw wins), but the current Potter and Empty Space setups failed and the score was below the Edge research threshold.
- `HOOD` bearish, Edge 12.46: positive seven-analog cohort (+0.2798R, 100% raw wins), but both current setup gates failed and the score was below threshold.
- `RIVN` bearish, Edge 0.00: setup failed and the analog cohort averaged -0.2578R.
- `CCL` bullish, Edge 0.00: setup failed and the analog cohort averaged -0.3814R.
- `PFE` bullish, Edge 0.00: setup failed and the analog cohort averaged -0.7033R.

The rejects are correct. Historical analog strength on `PLTR` and `HOOD` is not permission to bypass a failed present-tense setup. Tradier returned usable options evidence Friday, while low-confidence IEX equity bars remain research-grade rather than capital-ready.

## Intrinsic evidence-freshness fix

Found a live-gate fail-open in `scanner/main.py`: live preflight checked the filesystem modification time of `edge_audit_report.json`, but standalone `audit_edge` can rewrite that file from old validation and scan artifacts. A freshly rewritten `paper_trade_only` audit could therefore make two-day-old evidence appear current without a new market scan.

Fixed the evidence chain so:

- validation reports stamp `started_at`, `completed_at`, and the evidence-lab run ID;
- scan reports stamp the same evidence-lab run ID in addition to their existing timestamps;
- audit reports carry both source timestamps and both run IDs as intrinsic provenance;
- live preflight requires scan and validation to come from the same evidence-lab run, requires both intrinsic completion timestamps, and blocks evidence older than 24 hours or implausibly future-dated;
- the audit file-age check remains as an additional guard, but rewriting `audit_edge` alone can no longer refresh live eligibility.

The red regression reproduced the bug before the fix: a current-mtime audit built from two-day-old scan and validation timestamps passed live preflight. It fails closed after the change. Additional tests cover missing provenance and mixed evidence-lab runs.

The current July 31 audit predates these new provenance fields and is already `blocked`; it was not rewritten from after-hours inputs. Monday's full `research_ops` / `run_edge_lab` cycle will create the first provenance-complete canonical audit.

## Go-live evidence

Readiness is still blocked on `validation_threshold_55_unsupported` and `ranking_evidence_unsupported`:

- threshold 55 has one evidence day versus 20 required;
- pooled rank IC is +0.0098 with clustered p=0.378201;
- the top decile has 60 evidence days, +0.0235 mean daily R, HAC t=0.1742, and dependence-aware precision lower bound 0.3526;
- bullish evidence is unproven at +0.0633R / t=0.8205;
- bearish evidence is blocked at -0.1082R / t=-1.3276.

No global evidence route or direction gate passes.

## Verification

- Red stale-source regression failed before the fix and passed after it.
- Focused preflight/audit slice: 45 passed.
- Full suite: 324 passed with the two known all-NaN calibration warnings.
- `compileall`: passed.
- `pip check`: no broken requirements.
- Doctor: `status=ok`.

## Next run

Keep Monday's market-hours evidence cadence. Confirm the scheduled lab emits matching scan/validation run IDs and fresh intrinsic timestamps, then continue resolving the 14 pending counterfactuals. Do not loosen setup/readiness gates or enable live alerts until a dependence-aware global route and selected-direction gate pass.
