# Daily Kronos adjustment - 2026-07-29

## Verdict

NO-GO / `blocked`. Live trading and live promotion remain disabled.

Today's evidence did not justify a strategy threshold change. It did expose an options-promotion fail-open: Edge scoring described an above-limit spread as blocking, but the `promote` branch did not actually require the spread to pass. Scoring also hard-coded 12% instead of reading the current tunable spread limit.

## Scheduled market-hours cycle

- Windows task `Kronos Daily Research Ops` ran at 13:30 CT and completed successfully (`0x00000000`) in 441.172 seconds. Next run is 2026-07-30 at 13:30 CT; zero missed runs.
- Journal integrity before the scan was 913 input / 913 unique / 0 duplicates.
- Outcome review checked 12 pending rows and resolved none; no candidate had reached a valid completed-session outcome horizon.
- The learning brief remains at 49 resolved research candidates (26W/23L raw) and 17 pending.
- Adaptive policy held `RESEARCH_CANDIDATE_MIN_SCORE=65`: `hold_no_edge`, with no overrides applied.

## Research-only takes

Five bearish counterfactuals were logged; none is a trade or alert:

- `SOFI`, research score 68, Doctrine v2 80/pass, Kronos agreement 0.00.
- `CHPT`, research score 73, Doctrine v2 60/fail, Kronos agreement 0.30.
- `LUNR`, research score 73, Doctrine v2 60/fail, Kronos agreement 0.50.
- `RKLB`, research score 78, Doctrine v2 80/pass, Kronos agreement 0.20.
- `IONQ`, research score 78, Doctrine v2 80/pass, Kronos agreement 0.60.

Kronos remains advisory and contributes zero Edge score. The five rows are retained only for delayed, completed-session outcome measurement.

## Rejected trades

- Canonical market-hours Edge scan: 30 tickers, 16 active rejects, 14 skips, 0 Edge research candidates, 0 promotions.
- Top rejects: `RKLB` bearish 32.83, `IONQ` bearish 28.67, and `SOFI` bearish 27.92.
- All 16 active rows failed the setup gate and Edge research threshold; eight also had non-positive analog expectancy.
- `KEY`, `SOUN`, and `LUNR` had no Tradier contract clearing the liquidity gates. These are genuine untradeable-chain results, not wide observed quotes or broken market-data credentials.
- No rejected setup warranted loosening the Potter, Empty Space, analog-expectancy, or Edge score gates.

## Options promotion hardening

Two red regressions reproduced the defects:

- With the configured maximum spread tightened to 8%, a 10% spread still returned `promote`.
- A failed Tradier chain produced `wide_options_spread` plus `options_data_not_execution_grade` instead of the single root cause `options_no_liquid_contract`.

Fixed `scanner/edge/scoring.py` so:

- score penalties and blocking logic read the current `MAX_ATM_BID_ASK_SPREAD_PCT`;
- `promote` requires `options_passed` and a spread at or below that current limit, in addition to execution-grade quality;
- an authoritative failed Tradier lookup reports `options_no_liquid_contract` without inventing a spread or data-quality fault;
- a failed lookup with no authoritative provider reports `options_provider_unavailable`.

The market-hours scan had no promotion, so today's readiness verdict is unchanged. An after-close Edge lab was retained as evidence run `20260729T230624Z-95142d43`; because options quotes were then off-session, the canonical current reports were restored byte-for-byte from market-hours evidence run `20260729T183310Z-a7a66183`.

## Readiness evidence

- Readiness remains blocked on `validation_threshold_55_unsupported` and `ranking_evidence_unsupported`.
- Threshold 55 still has one independent evidence day / one raw signal, versus 20 required.
- Pooled rank IC is +0.0081 with entry-day-clustered p=0.399717.
- Top decile: 150 raw rows across 59 evidence days, +0.0467 mean daily R, HAC t=0.3453, dependence-aware precision lower bound 0.3681.
- Bullish: 66 evidence days, +0.0598 mean R, HAC t=0.7639 - unproven.
- Bearish: 65 evidence days, -0.1058 mean R, HAC t=-1.3102 - blocked.

Neither the global ranking route nor either direction clears the preregistered evidence gate.

## Verification

- Red test before the fix: two new options-scoring regressions failed.
- Focused Edge scoring/audit/CLI slice: 38 passed.
- Full suite: 318 passed, with the two known all-NaN calibration warnings.
- `compileall`: passed.
- `pip check`: no broken requirements.
- Doctor: `status=ok`.
- `git diff --check`: passed; only expected CRLF normalization notices.

## Next run

Keep the normal 13:30 CT market-hours cadence. Do not loosen setup or Edge thresholds and do not enable paper/live promotion until a global evidence route and the selected direction pass the dependence-aware gates. The corrected options gate must remain authoritative even if the liquidity autotuner changes the configured spread limit.
