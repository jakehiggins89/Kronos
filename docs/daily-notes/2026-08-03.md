# Daily Kronos adjustment - 2026-08-03

## Verdict

NO-GO / `blocked`. Live trading, live alerts, and paper promotion remain disabled.

Today's market-hours cycle produced the first complete intrinsic-provenance chain after the August 1 hardening. Scan and validation both carry evidence run ID `20260803T183418Z-92bb7dfb`, their completion timestamps are fresh, and the scheduled task remains healthy. The strategy still has no dependence-aware ranking edge, and today's code review found and closed an additional execution-quality fail-open before it could authorize live alerts.

## Scheduled market-hours cycle

- `Kronos Daily Research Ops` ran at 13:30 CT, returned `0x00000000`, and completed in 550.578 seconds. It has zero missed runs and is ready for Tuesday, 2026-08-04 at 13:30 CT.
- Journal integrity was 1,003 input / 1,003 unique / zero duplicates.
- Outcome review checked 14 pending rows and resolved four counterfactuals. No live outcome was resolved.
- Research scan checked 30 tickers: two research-only passes, 28 skips, and zero errors.
- The Edge lab indexed 11,055 records and evaluated 1,500 walk-forward candidates. Scan and validation provenance matched exactly.

## Accepted and resolved research evidence

Four July 24 bearish counterfactuals reached a complete five-session outcome window:

- `CHPT`, research score 73: stopped for -7.8724% / -1.0000R.
- `LUNR`, research score 73: horizon win of +6.6605% / +0.6532R.
- `RKLB`, research score 73: horizon win of +0.6657% / +0.0647R.
- `IONQ`, research score 68: stopped for -8.3523% / -1.0000R.

The two new research-only samples are both pending and cannot alert or trade:

- `GME` bearish, research score 78: Doctrine v2 43 / fail and Kronos agreement 0.00 / fail.
- `IONQ` bullish, research score 73: Doctrine v2 48 / fail and Kronos agreement 0.60 / fail.

The journal now has 57 resolved research candidates (29 wins / 28 losses raw) and 12 pending. After the same-ticker embargo, the score-65 cohort has 27 rows across 14 evidence days. The adaptive policy correctly held `RESEARCH_CANDIDATE_MIN_SCORE=65`; no threshold or tuning override changed.

## Rejected trades audited

The canonical Edge scan had 23 skips, seven active rejects, zero Edge research candidates, and zero promotions:

- `PFE` bullish, Edge 25.13: seven analogs averaged +0.9784R, but both current setup gates failed and the score remained below 45.
- `SNAP` bullish, Edge 5.26: setup failed and analog expectancy was -0.1498R.
- `LYFT` bullish, Edge 0.25: analog expectancy was only +0.0240R and both setup gates failed.
- `SOFI`, `GME`, `CCL`, and `IONQ`: all scored zero, failed both current setup gates, and had negative analog expectancy.

The rejects are correct. In particular, the research-only `IONQ` breakout is not permission to bypass its negative Edge analog cohort, failed Empty Space/Potter gates on the IEX snapshot, or the global readiness gate.

## Execution-quality authorization fix

Found a live-readiness fail-open in the boundary between Edge scoring and the readiness audit. Scoring intentionally permits the free Alpaca IEX equity feed (`feed_confidence=0.7`) for research, and a sufficiently strong row could still receive `recommendation=promote`. The audit reported `low_feed_confidence` only as a warning; if validation and direction evidence passed, that IEX row could make readiness `paper_trade_only`, which live preflight accepts. This contradicted the project's stated rule that IEX is research-grade rather than capital-grade.

Hardened `scanner/edge/audit.py` so a direction-eligible promoted row authorizes `paper_trade_only` only when it also has:

- consolidated equity-feed confidence of at least 0.75;
- a passed, fresh Tradier OPRA/OPRA-consolidated contract with options data quality at least 0.75;
- positive open interest, volume, and bid/ask spread evidence.

Rows that pass strategy/direction evidence but fail these execution checks now remain `research_only`, emit `promoted_candidates_execution_quality_blocked`, and are listed separately from genuinely execution-ready promotions. Research scoring and evidence collection are unchanged.

The red regression reproduced the old behavior (`paper_trade_only` on IEX) before the fix and passes after it. A pre-existing healthy-promotion fixture was also made explicit about its Tradier/OPRA provenance so the suite tests the intended execution-grade route.

## Go-live evidence

Readiness remains blocked on `validation_threshold_55_unsupported` and `ranking_evidence_unsupported`:

- threshold 55 has one evidence day versus 20 required;
- pooled rank IC is +0.0025 with entry-day-clustered p=0.466331;
- bullish within-direction IC is +0.0297 (p=0.235848), while bearish is -0.0396 (p=0.914422);
- the top decile has 150 raw rows across 62 evidence days, +0.0506 mean daily R, HAC t=0.4645, and dependence-aware precision lower bound 0.3853;
- bullish remains unproven and bearish remains blocked with negative expectancy.

No evidence route, selected-direction gate, or current candidate clears promotion. Even after the statistical gates eventually pass, IEX-only candidates will now remain research-only until a consolidated equity feed is available.

## Verification

- Red IEX authorization regression failed before the fix and passed afterward.
- Focused audit/ranking slice: 28 passed.
- Full suite: 325 passed with the two known all-NaN calibration warnings.
- Full-suite runtime isolation: zero files created, removed, or changed under `scanner/reports` and `scanner/tuning`.
- `compileall`: passed.
- `pip check`: no broken requirements.
- Doctor: `status=ok`.
- `git diff --check`: passed with CRLF normalization notices only.
- Audit regenerated from today's existing scan/validation artifacts only; no duplicate market observation was collected.

## Next run

Keep Tuesday's 13:30 CT market-hours cadence, resolve only complete five-session outcomes, and continue collecting dependence-aware walk-forward evidence. Do not loosen strategy/readiness gates or enable live alerts. If statistical promotion evidence eventually clears, the remaining operational prerequisite is a consolidated equity feed; free IEX data is now explicitly unable to authorize live-eligible readiness.
