# Daily Kronos adjustment - 2026-08-12

## Verdict

NO-GO / `blocked`. Live trading, live alerts, and paper promotion remain disabled.

Wednesday's market-hours cycle completed normally and produced no accepted setup, no Edge research candidate, and no promotion. Both validation directions remain negative after the pre-registered transaction-cost basis, and the score still does not rank outcomes. Today's code audit found and closed a cost-floor authorization bypass; it did not change the current metrics because the canonical evidence already uses the required 25 bps/side.

## Scheduled operating state

- The 13:30 CT `research_ops` cycle completed in 449.953 seconds with run ID `20260812T183343Z-0b1378a2`.
- The 14:30 recovery launch returned `skipped_already_complete` and retained the verified NYSE window (`13:30Z` open, `20:00Z` close).
- Task Scheduler reports `0x00000000`, zero missed runs, and the next launch Thursday, 2026-08-13 at 13:30 CT.
- Journal integrity was 1,183 input / 1,183 unique / zero duplicates.

## Accepted, rejected, and resolved trade review

The research scan accepted no new counterfactual today: 0 passes, 30 skips, and zero errors. Nothing was alerted or traded.

Outcome review resolved two August 4 bullish research-only counterfactuals:

- `LYFT` score 68: win, +5.2568% / +1.1306R at the horizon. Doctrine v2 passed at 80; Kronos agreement was 20% and failed.
- `IONQ` score 68: loss, -6.8247% / -1R at the stop. Doctrine v2 passed at 75; Kronos agreement was 30% and failed.

The research journal now has 71 resolved candidates (31 wins / 40 losses raw), 38 independent resolved windows (17 wins / 21 losses), and five pending. Adaptive policy held the research threshold at 65 with `hold_no_edge`.

The Edge scan had 23 skips and seven active rejects, with zero Edge research candidates and zero promotions:

- `IONQ` bullish led at 32.56; its seven net analogs averaged +0.7106R, but the current setup failed both Potter and Empty Space gates and remained below 45.
- `LUNR` bullish scored 18.50 with +0.2766R net analog expectancy; it also failed both setup gates and remained below 45.
- `GME` bearish scored 9.91 with -0.1742R net analog expectancy and failed the setup and research gates.
- `CHPT` and `KEY` additionally had no liquid Tradier contract. `F` and `TTD` scored zero.

Every active row failed the current setup gate and remained below the Edge research threshold. No reject merits an override.

## Go-live evidence

Readiness remains blocked on `validation_threshold_55_unsupported` and `ranking_evidence_unsupported`:

- all 1,500 validation rows have positive finite risk denominators and are net of 25 bps/side;
- threshold 55 has one evidence day versus 20 required;
- pooled net rank IC is -0.0206 with entry-day-clustered p=0.754208;
- the top decile spans 150 rows / 59 evidence days but averages -0.2817R per entry day, with HAC t=-2.6137 and dependence-aware precision lower bound 0.2535;
- bullish spans 64 days at -0.0691R / HAC t=-0.9866;
- bearish spans 64 days at -0.2072R / HAC t=-2.1477.

Both directions are blocked on negative net expectancy. More samples cannot justify live operation while the ranking sign and top-decile economics remain adverse.

## Cost-floor authorization fix

The August 8-11 changes required a positive, internally consistent cost stamp with complete R-denominator coverage, but `costs_charged` accepted any positive `bps_per_side`. A report recomputed at 0.01 bps/side could therefore satisfy the provenance check and pass an otherwise healthy evidence route even though 25 bps/side is the pre-registered production floor for this universe. That contradicted the documented guarantee that lowering `KRONOS_COST_BPS_PER_SIDE` cannot unlock readiness.

The audit now enforces an immutable 25 bps/side minimum in addition to basis, round-trip consistency, metric coverage, and denominator coverage. Below-floor or malformed reports receive the explicit `validation_cost_model_unsupported` blocker. The local and Telegram briefs label that condition as an actionable fault instead of claiming that any positive charge is a valid net basis. Lower-cost values remain usable for explicitly labeled research scenarios, but cannot authorize paper/live readiness.

The regression failed before the fix because 0.01 bps/side passed `costs_charged`; it passes after the fix. The canonical audit was regenerated from the saved Wednesday scan and validation only. Its 25/25 bps floor check passes, so the strategy metrics and NO-GO verdict are unchanged. No market scan, journal row, adaptive result, validation cohort, or tuning override was regenerated.

## Verification

- Focused audit/ranking/brief slice: 54 passed.
- Full suite: 349 passed with the two known NumPy all-NaN calibration warnings.
- Runtime report/tuning hash inventory across 1,337 files: zero created, removed, or changed by pytest.
- `compileall`: passed.
- `pip check`: no broken requirements.
- Doctor: `status=ok`.
- `git diff --check`: passed with CRLF normalization notices only.
- Protected scan, validation, journal, policy, research report, and tuning hashes were unchanged while regenerating the audit and local brief; Telegram delivery was explicitly disabled.

## Next run

Keep live trading, live alerts, and paper promotion disabled. Thursday should collect the next independent market-hours sample at the unchanged 25 bps/side production floor. Continue resolving only complete outcomes and do not loosen score, setup, direction, feed, liquidity, transaction-cost, or statistical gates.

Current run time: 2026-08-12T18:11:00-05:00.
