# Daily Kronos adjustment - 2026-07-16

## Verdict

Not live-ready. The final audit is still `blocked`, and live trading/live alerts stay disabled.

## Worktree/runtime note

The automation worktree at `C:\Users\Jacob Higgins\.codex\worktrees\ed38\kronos-predictor` has scanner source but no local `venv` and no ignored `scanner/reports` history. I used the full checkout at `C:\Users\Jacob Higgins\projects\kronos-predictor` with `venv\Scripts\python.exe` so the pass used the real journal and report state. Existing dirty sprint/statistics and prior daily-pass changes were preserved.

## Evidence refresh

Today's market-hours scheduled `research_ops` run was already current, so I audited that evidence rather than overwriting it with a less useful after-hours options scan.

- Started: `2026-07-16T18:30:02Z` (`2026-07-16 13:30` CT)
- Completed: `2026-07-16T18:36:27Z`
- Duration: 385.109 seconds
- Journal integrity at run start: 643 input records, 643 unique, 0 duplicates removed
- Post-scan journal check: 673 rows, 62 resolved, 12 pending, 599 not applicable, 0 duplicate tracked-sample fingerprints
- Outcome review: 10 pending checked, 2 counterfactual samples resolved, 0 live samples resolved, 0 quarantined, 0 bar-contract blocked
- Research scan: 30 tickers, 4 research-only detections, 26 skips, 0 errors
- Learning state: 34 resolved research candidates, 16 wins / 18 losses, 47.1% win rate, average 5-bar return 2.4754%, 12 pending
- Adaptive policy: `hold_no_edge`, threshold 65, no overrides applied
- Doctrine v2: `hold_doctrine_v2_baseline_pending_samples`; 20 resolved, current baseline cohort 6 wins / 1 loss, average return 9.9911%

Kronos lift remains too thin to use as a live gate: 8 scored rows, 3 eval errors, agree cohort n=1, disagree cohort n=7.

## Rejected/taken trade review

Research-only detections:

- `SOFI` bearish, research score 68, source session 2026-07-16, latest source timestamp `2026-07-16T14:00:00-04:00`, entry 17.45.
- `LUNR` bearish, research score 73, source session 2026-07-16, latest source timestamp `2026-07-16T14:00:00-04:00`, entry 13.4699.
- `RKLB` bearish, research score 68, source session 2026-07-16, latest source timestamp `2026-07-16T14:00:00-04:00`, entry 67.28.
- `IONQ` bearish, research score 78, source session 2026-07-16, latest source timestamp `2026-07-16T14:00:00-04:00`, entry 35.13.

These are counterfactual research samples only. None is a live alert.

Active edge rejects:

- 30 tickers scanned: 15 rejects, 15 skips, 0 research/promoted live candidates.
- Top active rejects: `RKLB` bearish edge 8.57, `PFE` bullish edge 6.05, `CIFR` bearish edge 3.97, `F` bullish edge 2.50, `BBAI` bearish edge 1.07.
- Reject mix: `setup_gate_failed` 15, `edge_score_below_research_threshold` 15, `non_positive_analog_expectancy` 10, `wide_options_spread` 4, `options_data_not_execution_grade` 4.

The reject mix still does not justify threshold loosening. Every active candidate failed the setup gate and missed the research edge threshold; most bearish candidates also carried negative analog expectancy.

## Options evidence check

The intraday scan still warned on options execution quality, so I checked the credential path without printing secrets. `scanner\.env` is ignored and contains `TRADIER_API_TOKEN`, plus Alpaca and Telegram keys. The current options warnings are therefore not from a missing token path.

The four candidates with `options_data_not_execution_grade` were `RIOT`, `UPST`, `LUNR`, and `EVGO`. They used `tradier` / `opra-consolidated`, but the selected contract evidence failed closed with spread 1.0, quote age 9999, and zero open interest / volume. That is a data-quality blocker, not a reason to loosen the options gate.

## Audit

Readiness remains `blocked`.

Blockers:

- `validation_threshold_55_unsupported`
- `ranking_evidence_unsupported`

Warnings:

- `low_feed_confidence`
- `options_liquidity_missing`
- `options_data_not_execution_grade`
- `no_current_actionable_candidates`
- `bearish_edge_negative`

Ranking evidence is still unsupported: rank IC -0.0164 vs required 0.07, p 0.737067, bullish within-direction IC -0.0284, bearish within-direction IC -0.0243, top-decile average R 0.0183, top-decile t-stat 0.1621, Wilson lower-bound precision 0.3685. Threshold 55 still has 0 supporting signals versus the required 20.

Purge protection remains fail-closed at 11 calendar days: `embargo_days=11`, `cross_ticker_embargo_days=11`, and `min_required_days=11`.

## Code changes

No code change was warranted today. The July 15 sample-class fingerprint fix held: tracked-sample duplicate groups stayed at 0 after today's scheduled run, and the journal kept the 12 pending tracked research samples distinct from `not_applicable` rows.

## Verification

- Full suite: 282 passed, 2 known all-NaN calibration warnings
- `compileall`: passed
- `pip check`: no broken requirements
- `scanner.main --mode doctor`: `status=ok`
- `git diff --check`: passed with CRLF normalization warnings only
- Secret-assignment scan on this note: no matches

## Next action

Keep live disabled. Continue market-hours `research_ops` so the 12 pending research samples can mature. If the same Tradier execution-grade warning persists on another intraday run with the token present, inspect the raw chain response for `RIOT`, `UPST`, `LUNR`, and `EVGO` without logging token values; distinguish genuinely illiquid contracts from a quote timestamp or contract-selection parsing bug before touching thresholds.
