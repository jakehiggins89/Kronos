# Daily Kronos adjustment - 2026-08-04

## Verdict

NO-GO / `blocked`. Live trading, live alerts, and paper promotion remain disabled.

Today's scheduled market-hours run was healthy and its scan/validation provenance matched. The strategy still lacks dependence-aware out-of-sample ranking evidence. The trade decisions were defensible, but a review of the final authorization boundary found another concrete execution-quality fail-open: a high-confidence but deliberately delayed consolidated equity feed could still authorize `paper_trade_only`. That route is now closed.

## Scheduled market-hours cycle

- `Kronos Daily Research Ops` ran at 13:30 CT and completed in 493.469 seconds with result `0x00000000`, zero missed runs, and its next run scheduled for Wednesday, 2026-08-05 at 13:30 CT.
- Journal integrity was 1,033 input / 1,033 unique / zero duplicates before today's 30-row research scan was appended.
- Outcome review checked 12 pending counterfactuals and resolved one. No live outcome was resolved.
- Research scan checked 30 tickers: two research-only passes, 28 skips, and zero errors.
- Edge evidence run `20260804T183334Z-4ab5d3bd` produced matching scan and validation provenance.

## Accepted and resolved research evidence

The one newly complete five-session counterfactual was a loss:

- `CHPT` bearish from July 27, research score 68: -6.3992% / -0.8677R at the horizon. Doctrine v2 scored 60 and failed; Kronos agreement was 0.40 and failed.

Today's two accepted research-only samples are pending and cannot alert or trade:

- `LYFT` bullish, research score 68: Doctrine v2 80 / pass, Kronos agreement 0.20 / fail, entry 16.645.
- `IONQ` bullish, research score 68: Doctrine v2 75 / pass, Kronos agreement 0.30 / fail, entry 41.895.

The journal now has 58 resolved research candidates (29 wins / 29 losses raw) and 13 pending. At the active score-65 threshold, dependence-aware evidence is 27 independent rows across 14 entry days, +2.8854% average daily return, HAC t=1.7004, and lower-bound win rate 0.4791. The adaptive policy correctly held threshold 65.

The direction split remains interesting but is not permission to trade: the bearish research cohort has 19 rows across 12 days, +5.3900% average daily return, HAC t=2.3036, and lower-bound win rate 0.5524, while the bullish research cohort is negative. This research-scanner cohort is distinct from the Edge validation cohort, where bearish expectancy remains negative; no gate was loosened.

## Rejected trades audited

The canonical Edge scan had 19 skips, 11 active rejects, zero Edge research candidates, and zero promotions. Every reject failed the current setup gates and remained below the Edge research threshold; seven also had non-positive analog expectancy.

- `LYFT` bullish led at Edge 44.00. Its seven analogs were all winners and averaged +1.1458R, but both live setup gates failed and the score stayed below 45. It is correctly research-only in the separate Potter scan, not an Edge promotion.
- `SNAP` bullish scored 17.82 with +0.1141R analog expectancy, but both setup gates failed.
- `AAL` bullish scored 6.27 with -0.0822R expectancy and both setup gates failed.
- `PLTR`, `PFE`, `BBAI`, and `IONQ` scored 5.17, 4.11, 4.01, and 0.10 respectively; none cleared a setup gate or Edge threshold.
- `SOFI`, `SOUN`, `GME`, and `CCL` scored zero. Their rejection is correct.

Tradier OPRA evidence was healthy for all 11 active candidates: no provider outage, stale quote, missing-liquidity, or non-execution-grade options warning appeared. Equity evidence remained free Alpaca IEX and therefore research-grade only.

## Delayed-equity authorization fix

Yesterday's audit hardening correctly prevented low-confidence IEX equity rows from authorizing `paper_trade_only`, but it checked only equity feed confidence and generic quality. Alpaca's free historical SIP path has high consolidated-feed confidence while intentionally setting `data_delay_minutes=16`. A promoted candidate backed by that delayed snapshot plus healthy Tradier options data could therefore pass `_candidate_execution_ready()` and produce a readiness value consumed by live preflight.

Hardened `scanner/edge/audit.py` so current equity-delay provenance must be present, finite, nonnegative, and at most one minute before a promoted row can authorize `paper_trade_only`. Missing provenance fails closed. Delayed consolidated candidates remain valid research samples, receive the explicit `delayed_equity_feed` warning, and are listed in the audit summary.

Updated the daily brief translation and `scanner/README.md` so this state is reported as a normal research limitation rather than a broken provider. A red regression demonstrated the previous fail-open (`paper_trade_only` on a 16-minute SIP snapshot); the same case now returns `research_only`. The regression also covers missing delay provenance.

Regenerated only `audit_edge` and the daily brief from today's existing scan/validation artifacts. No duplicate market scan was collected. Today's IEX candidates remain blocked by low feed confidence, so the canonical readiness and candidate decisions did not change.

## Go-live evidence

Readiness remains blocked on `validation_threshold_55_unsupported` and `ranking_evidence_unsupported`:

- threshold 55 has one evidence day versus 20 required;
- pooled rank IC is +0.0012 with entry-day-clustered p=0.483187;
- bullish within-direction IC is +0.0249 (p=0.264522), while bearish is -0.0368 (p=0.894253);
- the top decile has 150 raw rows across 62 evidence days, +0.1103 mean daily R, HAC t=0.9561, and dependence-aware precision lower bound 0.4013;
- bullish Edge expectancy remains unproven and bearish Edge expectancy remains blocked.

No validation route, direction gate, or current candidate clears promotion. Even if the statistical gates eventually pass, execution authorization now requires effectively real-time consolidated equity data as well as healthy Tradier OPRA evidence.

## Verification

- Red delayed-SIP authorization regression failed before the fix (`paper_trade_only`) and passed afterward (`research_only`).
- Focused audit/ranking/brief slice: 45 passed.
- Full suite: 327 passed with the two known NumPy all-NaN calibration warnings.
- Full-suite runtime isolation: zero files created, removed, or changed under `scanner/reports` and `scanner/tuning`.
- `compileall`: passed.
- `pip check`: no broken requirements.
- Doctor: `status=ok`.
- `git diff --check`: passed with CRLF normalization notices only.

## Next run

Keep the Wednesday 13:30 CT market-hours cadence, resolve only complete outcomes, and continue collecting dependence-aware evidence without loosening the strategy or readiness gates. Recheck the emerging bearish research-score cohort as independent days accumulate, but do not confuse it with the still-negative bearish Edge cohort. Live trading and live alerts remain disabled.
