# Daily Kronos adjustment - 2026-08-11

## Verdict

NO-GO / `blocked`. Live trading, live alerts, and paper promotion remain disabled.

This audit ran before today's 13:30 CT scheduled evidence cycle, so it reviewed Monday's completed market-hours run and did not replace the canonical reports with an early duplicate. Monday produced no live or Edge-qualified trade, and the score still has no credible net ranking edge. Two fail-closed mismatches were fixed before today's scheduled run: analog expectancy was scored gross while readiness was judged net, and the recovery launcher erased verified NYSE session timestamps from its final status.

## Monday accepted and rejected trade review

The only accepted row was a counterfactual research sample, never a trade:

- `TTD` bearish, research score 68: Doctrine v2 passed at 80, but Kronos predicted the opposite direction with 0% agreement (`+24.75%` median forecast versus the bearish setup). `final_pass=false`; no alert or execution occurred.

Outcome review checked nine pending samples and resolved one counterfactual loss. The research journal now has 67 resolved candidates (29 wins / 38 losses raw), 36 independent resolved rows (16 wins / 20 losses), and nine pending. The adaptive policy correctly held the research threshold at 65.

The Edge scan had 22 skips and eight active rejects, with zero Edge research candidates and zero promotions:

- `PLTR` bullish led at 24.71, but failed both live setup gates and the Edge research threshold.
- `PFE` bullish scored 20.29 with positive gross analog expectancy, but failed both setup gates and the research threshold.
- `CLSK` bearish scored 9.02 and additionally had negative analog expectancy.
- `TTD`, `GME`, `MARA`, `LYFT`, and `CIFR` were also correctly rejected; `LYFT` had no liquid Tradier contract.

No rejected setup merits an override. The execution feed remains research-grade IEX, and all active scores were far below 45 before today's hardening.

## Go-live evidence

Readiness remains blocked on `validation_threshold_55_unsupported` and `ranking_evidence_unsupported` after charging 25 bps per side:

- threshold 55 has one evidence day versus 20 required;
- pooled rank IC is -0.0014 with entry-day-clustered p=0.517370;
- the top decile spans 59 evidence days but averages -0.1473 R, with HAC t=-1.3920 and dependence-aware precision lower bound 0.2786;
- bullish averages about -0.05 R per entry day and bearish about -0.25 R; both directions are blocked.

The strategy is not close to go-live: its scores do not rank net outcomes, its top tail loses after costs, and neither direction has positive statistically supported expectancy.

## Net analog-scoring fix

The August 8/9 transaction-cost work made every readiness metric net of 25 bps per side, but `score_edge_candidate()` still built its analog expectancy, Edge score, and promotion recommendation from gross retrieval outcomes. That meant candidate selection and validation were evaluating different strategies: a thin gross edge could earn a high score or even a promotion recommendation before the audit judged net results.

Candidate scoring now applies the same round-trip cost to every retrieved analog before computing average return, R multiple, win rate, analog expectancy, Edge score, and recommendation. Gross summaries remain visible for diagnosis. Each result carries an `analog_cost_model` block, and missing/nonpositive analog risk denominators block promotion instead of silently retaining gross R.

Replaying Monday's exact candidates against the saved 11,040-row index shows the practical effect:

- `PLTR`: 24.71 gross-basis score -> 23.71 net-basis score.
- `PFE`: 20.29 -> 14.63; its analog win rate changes from 57.1% gross to 42.9% net.
- `CLSK`: 9.02 -> 8.10.
- `TTD`: 4.91 -> 4.08.

All remain rejects. The fix does not loosen any gate or manufacture candidates; it removes optimism before a candidate can approach promotion.

## Scheduler provenance fix

Monday's 13:30 CT run completed successfully, and the 14:30 recovery launch correctly skipped it. However, that recovery no-op overwrote `scheduled_research_ops_status.json` without the previously verified NYSE open/close timestamps. The code now resolves and validates the exchange session before the already-complete fast path, then retains `market_open_at` and `market_close_at` in the final no-op status. Calendar failures still block the run.

Task Scheduler is healthy (`0x00000000`, zero missed runs); today's next launch is 13:30 CT.

## Verification

- Red analog-cost regression failed before the fix: three +0.20% gross wins remained wins instead of becoming -0.30% net losses.
- Red scheduler regression failed before the fix because recovery status lacked `market_open_at`.
- Focused Edge suite: 92 passed with the two known NumPy all-NaN calibration warnings.
- Scheduler suite: 7 passed.
- Full suite: 346 passed with the same two known warnings.
- Operational report/tuning hash inventory across 1,301 files: zero created, removed, or changed by pytest.
- `compileall`: passed.
- `pip check`: no broken requirements.
- Doctor: `status=ok`.
- `git diff --check` and targeted secret-assignment scan: passed (CRLF normalization notices only).

## Next run

Keep all live paths disabled. Let today's normal 13:30 CT scheduled cycle collect the first canonical evidence using net analog scoring, then verify the 14:00/14:30 recovery status retains the NYSE session timestamps. Do not loosen score, setup, cost, feed, options-liquidity, or statistical gates in response to continued zero promotions.
