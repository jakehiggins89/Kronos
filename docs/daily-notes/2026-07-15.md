# Daily Kronos adjustment - 2026-07-15

## Verdict

Not live-ready. The final audit is still `blocked`, and live trading/live alerts stay disabled.

## Worktree/runtime note

The automation worktree at `C:\Users\Jacob Higgins\.codex\worktrees\185c\kronos-predictor` had scanner source code but no local `venv` and no ignored `scanner/reports` history. I used the full checkout at `C:\Users\Jacob Higgins\projects\kronos-predictor` with `venv\Scripts\python.exe` so the evidence pass used the real journal and report state. Existing dirty sprint/statistics and prior daily-pass changes were preserved.

## Evidence refresh

Final `research_ops` run:

- Started: `2026-07-16T03:13:05Z` (`2026-07-15` evening CT)
- Completed: `2026-07-16T03:18:15Z`
- Duration: 309.984 seconds
- Journal integrity at run start: 642 input records, 642 unique, 0 duplicates removed
- Post-scan journal check: 643 rows, 10 pending tracked research samples, 0 duplicate tracked-sample fingerprints
- Outcome review: 9 pending checked, 0 resolved, 0 quarantined, 0 bar-contract blocked
- Research scan: 30 tickers, 3 research-only detections, 27 skips, 0 errors
- Research-only setup detections: `F` bullish score 73, `LUNR` bearish score 73, and `IONQ` bearish score 73
- Learning state: 32 resolved research candidates, 14 wins / 18 losses, 43.75% win rate, average 5-bar return 1.4355%, 10 pending
- Adaptive policy: `hold_no_edge`, threshold 65, no overrides applied
- Doctrine v2: `hold_doctrine_v2_baseline_pending_samples`; 18 resolved, current baseline cohort 5 wins / 1 loss, average return 8.589%

Kronos lift remains too small to use as a live gate: 6 scored rows, 3 eval errors, agree cohort n=1, disagree cohort n=5.

## Rejected/taken trade review

Research-only detections:

- `F` bullish, research score 73, source session 2026-07-15, latest source timestamp `2026-07-15T14:00:00-04:00`, entry 14.21.
- `LUNR` bearish, research score 73, source session 2026-07-15, latest source timestamp `2026-07-15T19:30:00-04:00`, entry 14.5506.
- `IONQ` bearish, research score 73, source session 2026-07-15, latest source timestamp `2026-07-15T14:00:00-04:00`, entry 37.81.

These are counterfactual research samples only. None is a live alert.

Active edge rejects:

- 30 tickers scanned: 13 rejects, 17 skips, 0 research/promoted live candidates.
- Top active rejects: `AAL` bearish edge 25.48, `KEY` bullish edge 21.98, `NIO` bullish edge 15.20, `PFE` bullish edge 12.91, `LUNR` bearish edge 10.04.
- Reject mix: `setup_gate_failed` 13, `options_data_not_execution_grade` 13, `edge_score_below_research_threshold` 13, `non_positive_analog_expectancy` 8, `wide_options_spread` 4.

The reject mix still does not justify threshold loosening. The top candidates are blocked by setup gates, execution-grade options data, insufficient edge score, and in several cases negative analog expectancy.

## Bug fixed

Found a daily journal blind spot while inspecting taken trades. Earlier today, `LUNR` was saved as a same-session `not_applicable` research row at score 55. Tonight it crossed the research threshold at score 73, but the source-session fingerprint treated the below-threshold row and the true tracked sample as the same identity, so `append_decision()` initially skipped the candidate.

Fix shipped in `scanner/learning/outcome_store.py`:

- Added a journal sample class to `decision_fingerprint()` so `not_applicable` rows cannot suppress later `pending` or `resolved` tracked samples from the same ticker/session.
- Kept the existing dedupe behavior for real tracked samples, so repeated same-session research candidates still merge.
- Added regression coverage in `scanner/tests/test_outcome_store.py` for append-time and cleanup-time behavior.

After the fix and rerun, `LUNR` was captured as a pending July 15 research sample and duplicate tracked-sample groups stayed at 0.

## Audit

Readiness remains `blocked`.

Blockers:

- `validation_threshold_55_unsupported`
- `ranking_evidence_unsupported`

Warnings:

- `low_feed_confidence`
- `options_liquidity_missing`
- `options_data_not_execution_grade`
- `no_current_actionable_candidates`
- `bearish_edge_negative`

Ranking evidence is still unsupported: rank IC -0.0164 vs required 0.07, p 0.737067, bullish within-direction IC -0.0284, bearish within-direction IC -0.0243, top-decile average R 0.0183, top-decile t-stat 0.1621, Wilson lower-bound precision 0.3685. Threshold 55 still has 0 supporting signals versus the required 20.

## Verification

- Focused outcome-store regression: 8 passed
- Full suite: 282 passed, 2 known all-NaN calibration warnings
- `compileall`: passed
- `pip check`: no broken requirements
- `scanner.main --mode doctor`: `status=ok`
- `git diff --check`: passed with CRLF normalization warnings only

## Next action

Keep live disabled. Continue market-hours `research_ops` so options quotes are fresher and the 10 pending research samples can mature. If `options_data_not_execution_grade` persists during an intraday run, verify `TRADIER_API_TOKEN` and quote freshness before touching thresholds.
