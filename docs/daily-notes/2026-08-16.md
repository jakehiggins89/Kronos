# Daily Kronos adjustment - 2026-08-16

## Verdict

NO-GO / `blocked`. Keep live trading, live alerts, and paper promotion disabled.

Today is Sunday, so I preserved Friday's canonical scan, validation, journal, policy, brief, and Telegram artifacts. Task Scheduler remains healthy (`0x00000000`, zero missed runs); the next launch is Monday, 2026-08-17 at 13:30 CT.

## Friday evidence review

Friday remains one accepted research-only counterfactual (`T` bullish), two scan-level Edge recommendations (`T` and `AFRM` bullish), eight active rejects, 20 skips, zero paper-authorized candidates, and zero live trades. The two recommendations remain research only because bullish validation is negative and their IEX equity feed is not execution grade. Every reject remains below the corrected strategy threshold or fails its setup gate; `KEY` also has no liquid options contract. No reject merits an override.

The current evidence remains decisively NO-GO. The corrected-score read-only replay from Saturday put threshold 55 at 56 raw signals / 41 entry days, -0.2760R per day, HAC t=-2.9396, and a dependence-aware precision lower bound of 0.2052. Rank IC remained unsupported at +0.0196 with clustered p=0.282282, and the top decile remained -0.1600R per day. Both bullish and bearish direction economics are negative.

## Runtime-source provenance bug

I found a live-safety gap exposed by Saturday's score-scale correction. Live preflight required a report file under 24 hours old, matching intrinsic scan/validation timestamps, and one evidence-lab run ID, but it did not prove that those reports were produced by the source code currently about to execute. A previously passing audit could therefore remain live-eligible for up to 24 hours after scoring, strategy, cost, or execution code changed.

This weekend is the concrete reproduction: Friday's canonical reports have valid matching run IDs and timestamps but were generated before Saturday's scorer changed. Under the new in-memory audit they fail closed with `evidence_runtime_source_mismatch`; the stored Friday artifacts were not rewritten.

The fix adds a deterministic SHA-256 fingerprint over production scanner Python source while excluding tests, generated reports/logs, and one-off experiment harnesses. Validation and scan reports now stamp the fingerprint independently, the evidence manifest records the starting fingerprint, and audit requires both reports to match each other and the source being audited. Live preflight recomputes the current fingerprint and refuses missing, mixed, or stale-source evidence. The hash reads working-tree bytes, so uncommitted production edits invalidate readiness too.

## Verification

- Red regressions reproduced both failures before the fix: live preflight accepted a fresh audit stamped with different source, and audit generation did not detect mixed scan/validation source.
- Focused provenance/preflight/evidence-lab suite: 23 passed.
- Full suite: 357 passed with the two known NumPy all-NaN calibration warnings.
- Runtime report/tuning inventory: 1,373 files, aggregate SHA-256 unchanged before and after tests.
- `compileall`: passed.
- `pip check`: no broken requirements.
- Doctor: `status=ok`.
- `git diff --check`: passed with CRLF normalization notices only.
- Targeted tracked-diff secret-assignment scan: no matches.

## Next run

Monday should generate the first canonical evidence set that is both on the historically comparable score scale and cryptographically bound to the current runtime source. Confirm validation and scan fingerprints match, confirm candidate gross/net analog provenance is present, and keep every live/paper path disabled unless the negative net evidence genuinely reverses without loosening a gate.

Current run time: 2026-08-16T18:09:10-05:00.
