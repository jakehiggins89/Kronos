# Daily Kronos adjustment - 2026-07-20

## Verdict

Not live-ready. The market-hours audit remains `blocked`; live trading and live alerts stay disabled.

## Runtime and evidence choice

The automation worktree at `C:\Users\Jacob Higgins\.codex\worktrees\61d0\kronos-predictor` is detached at the July 3 project state and has no operational report history. I used the operational checkout at `C:\Users\Jacob Higgins\projects\kronos-predictor` with `venv\Scripts\python.exe`, preserving all existing July sprint, statistics, journal, and daily-note work.

Today's scheduled market-hours `research_ops` run was already current, so I audited it rather than replacing its intraday options evidence with an evening scan.

- Started `2026-07-20T18:30:03Z`; completed `2026-07-20T18:38:56Z`; duration 533.484 seconds.
- Journal integrity: 703 input / 703 unique / 0 duplicates removed before the scan.
- Initial outcome review checked 10 pending samples and resolved 0.
- Research scan: 30 tickers, 2 research-only detections, 28 skips, 0 errors.
- New tracked counterfactuals: `CHPT` bearish, score 70, entry 5.62; `IONQ` bearish, score 65, entry 35.19. Both had low Kronos agreement (0.30 and 0.40), failed Doctrine v2, and remain research-only.
- Journal after the scan: 37 resolved research candidates, 18 wins / 19 losses, 12 pending.
- Adaptive evidence remains 24 independent resolved windows; the active threshold-65 cohort remains 10 wins / 9 losses with Wilson lower bound 0.3835. No override was applied.

## Rejected and taken trade review

There were no live or paper-trade promotions.

- Edge scan: 30 tickers, 12 active rejects, 18 skips, 0 research/promoted edge candidates.
- Top rejects: `SOFI` bearish edge 18.08, `GME` bearish 13.48, and `RKLB` bearish 11.53.
- Every active candidate failed the strict setup gate and remained below the edge research threshold; 8 also had non-positive analog expectancy.
- `CHPT` and `EVGO` had no liquid options contract. Their wide-spread/non-execution-grade rejection remains correct.
- `CHPT` and `IONQ` can be valid graded Potter research samples while still being invalid edge/live candidates; the research journal records counterfactuals before the options, analog, and strict setup promotion gates.

Readiness blockers remain:

- `validation_threshold_55_unsupported`: 0 supporting signals versus 20 required.
- `ranking_evidence_unsupported`: rank IC -0.0109 (p 0.663935), bullish within-direction IC -0.0188, bearish IC -0.0234, top-decile average R +0.0300, t-stat 0.2633, and Wilson lower-bound precision 0.3685.

Bearish validation remains negative, so bearish promotion stays blocked.

## Bug fixed: outcome sessions were shifted and could be incomplete

The outcome reviewer compared a decision timestamp directly with synthetic-session index labels. Those labels identify the session start: under the default 20:00 anchor, a Monday 14:00 source bar is labelled Sunday 00:00. Comparing the Monday 14:32 decision timestamp selected the Monday 00:00 label, whose bars actually come from Tuesday, shifting the five-session outcome window forward.

The reviewer also accepted a target synthetic session as soon as its first intraday bars existed. A 13:30 CT scheduled run could therefore resolve a five-session outcome using a partial 14:00 ET bar before the rest of the session could hit the stop or change the horizon close.

Changes in `scanner/learning/outcome_reviewer.py`:

- Source-aware records now map `latest_source_timestamp` through the same anchor rule as `build_synthetic_sessions` and require the exact source session.
- Legacy rows without source timestamps retain the prior fallback because their true source session cannot be reconstructed safely.
- A target session cannot resolve until its closing anchor plus provider delay has elapsed.
- Resolved source-aware rows without the new alignment version fail closed to pending and self-repair before they can continue influencing autonomous tuning.
- Outcome-review summaries expose alignment repairs reviewed, resolved, and still pending.

Added regressions in `scanner/tests/test_outcome_reviewer.py` for shifted source labels, partial terminal sessions, and self-repair of an already resolved row.

## Real-journal repair

Backed up the journal to `scanner/reports/scan_decisions_backup_outcome_alignment_20260720T181004.jsonl`, then ran the corrected reviewer.

- 14 rows reviewed: 12 pending plus 2 source-aware alignment repairs.
- Both repairs resolved; 0 remain pending and no rows were quarantined or blocked by the bar contract.
- `LUNR` 2026-07-10 remained a win, but its completed-session return changed from +14.2145% to +13.9651%.
- `IONQ` 2026-07-10 remained a win, but its completed-session return changed from +18.9488% to +19.3177%.
- Re-running `adaptive_policy --apply_tuning` returned `no_overrides_applied`. The research recommendation remains `hold_no_edge`; Doctrine v2 remains `hold_doctrine_v2_baseline_pending_samples`.

## Verification

- Outcome/alignment regression file: 7 passed.
- Related outcome, triple-barrier, adaptive-policy, and journal tests: 55 passed.
- Full suite: 303 passed with the same 2 known all-NaN calibration warnings.
- `compileall`: passed.
- `pip check`: no broken requirements.
- `scanner.main --mode doctor`: `status=ok`.
- Focused `git diff --check`: passed with CRLF normalization notices only.

## Next action

Keep live disabled. Let the next market-hours cycle verify the self-repair counters return to zero and resolve the July 13+ samples only after their exact five-session target windows are complete. Do not loosen setup, ranking, direction, or options gates while both readiness blockers remain.
