# Daily Kronos adjustment - 2026-08-19

## Verdict

NO-GO / `blocked`. Keep live trading, live alerts, and paper promotion
disabled.

Today's market-hours run successfully rebuilt the corrected Doctrine v2 / Edge
feature-v4 corpus with matching runtime fingerprints. The correction removed
false punchback-reclaim support but did not reveal positive edge. Both
production evidence routes remain adequately sampled and negative. A
pre-registered close-based structural-stop experiment also failed three of
four gates, so no entry, stop, score, promotion, or alert change is authorized.

## Scheduled run and learning journal

The 13:30 CT scheduled `research_ops` cycle completed normally in 408.75
seconds with run ID `20260819T183310Z-a6c8c0d0`. The 14:30 recovery launch
returned `skipped_already_complete`. Task Scheduler reports `0x00000000`, zero
missed runs, and the next launch Thursday at 13:30 CT.

Journal integrity was 1,333 input / 1,333 unique. Outcome review checked all
four pending counterfactuals and resolved none. The research cohort remains 76
resolved candidates (34W/42L, 44.7% raw win rate) and four pending. Adaptive
policy held the research threshold at 65 with `hold_no_edge`; no tuning
override changed. Corrected Doctrine evidence version 2 has no resolved rows
yet, so the policy correctly excludes 62 legacy rows rather than mixing the old
and corrected semantics.

The strict research scan had zero passes, 30 skips, and zero errors. No new
Potter research counterfactual was accepted today.

## Accepted and rejected Edge rows

The Edge scan produced 20 skips, nine active rejects, one research-only
recommendation, zero promotions, zero paper-authorized candidates, and zero
live trades.

- `LYFT` bullish was accepted for research only at Edge 46.52. Its seven
  analogs averaged +0.3160 net R (+0.3887 gross R), but it remained below the
  promotion threshold, failed Empty Space, carried Doctrine 68
  `fresh_breakout`, and did not authorize an alert or trade.
- `PFE` bullish (-0.012 net analog R) and `T` bullish (-0.536R) were correctly
  rejected for non-positive expectancy and insufficient score.
- `CLSK` (+0.377R), `KEY` (+0.078R), and `GME` (+0.038R) had positive
  seven-analog point estimates, but all failed the live Potter/Empty Space setup
  gate and stayed below the research score. `KEY` also had no liquid contract.
  Sparse analog optimism does not override a failed setup.
- `AAL` (-0.503R), `CCL` (-0.032R), `CIFR` (-0.454R), and `EVGO` (-0.330R)
  failed setup and had non-positive net expectancy. `EVGO` also had no liquid
  contract.

No rejected row merits an override.

## Corrected feature-v4 readiness

The index contains 11,030 feature-v4 rows and validation used the most recent
1,500 purged walk-forward candidates, all with complete 25 bps/side cost
coverage. Scan and validation shared current market-hours fingerprint
`sha256:7a0d7292ae4f165ed59ce44d6918b31f26b369fa915bc2ac1dd3bef81f656773`.

- Score 55: 32 raw signals / 27 independent entry days, -0.2434R/day,
  HAC t=-2.1654, dependence-aware precision lower bound 0.2155 versus 0.55.
- Ranking: IC +0.0362 with clustered p=0.139661 versus +0.07 / p<=0.05.
- Top decile: 150 raw / 55 independent entry days, -0.2102R/day,
  HAC t=-2.1188, precision lower bound 0.2932 versus 0.45.
- Bullish and bearish direction blocks remain negative.

Readiness remains blocked on `validation_threshold_55_unsupported` and
`ranking_evidence_unsupported`. This is mature negative evidence, not a sample
shortage, and neither threshold should be loosened.

## Close-based structural-stop experiment

I pre-registered one source-derived stop rule before reading its result: keep
the exact source-valid signal, entry, Potter cost-basis stop level, risk
denominator, five-session horizon, no-target plan, and 25 bps/side cost, but
invalidate only after a completed daily close beyond cost basis and fill at
that observed close. This tests the doctrine's close-over-wick priority while
allowing delayed exits to lose more than 1R.

The cached-bar production control passed across 1,373 non-overlapping rows
(correlation 0.99999657, mean difference +0.00007286R). The bullish rule was
falsified:

- Coverage passed: 751 rows / 314 independent entry days.
- Absolute edge failed: -0.0189R/day, HAC t=-0.2671.
- Same-setup improvement failed: -0.0029R/day versus the shipped wick stop,
  t=-0.2337.
- Day-matched drift failed: -0.0022R/day, t=-0.0445.
- Bearish remained negative at -0.1151R/day, t=-1.5505.

The raw per-row mean looked better (+0.0273R versus +0.0064R), but those gains
clustered on favorable days and vanished under the pre-registered independent-
day analysis. Do not use the unclustered mean to rescue the rule. The exact
daily-close/cost-basis rule is closed; no production change is authorized.
Full preregistration, pinned hashes, runner, results, and report are under
`scanner/research/experiments/20260819_close_invalidation_stop/`.

## Operator-message bug

Yesterday's detailed brief fix distinguished mature-negative gates from
under-sampled gates, but the concise Telegram block still rendered `UNLOCK -
not yet` and said the score did not rank winners "yet." That contradicted the
same message's negative-evidence headline and could invite more sample
collection or threshold loosening after the design was already falsified.

`scanner/brief.py` now renders `UNLOCK - current design negative` and `ranking
evidence is negative out of sample` when both adequately sampled routes are
negative. A red regression reproduced the old wording; the full 24-test brief
suite passes. I did not resend today's Telegram or regenerate canonical reports
after hours.

Because the source fingerprint intentionally includes runtime reporting code,
the messaging change moved the current source fingerprint to
`sha256:3bfc55a5f88389886bbde22fce0233c200d1030594c7bad8f78ad0fc623b3343`.
It no longer matches the stored market-hours fingerprint, so live preflight
fails closed until Thursday's scheduled rebuild. This does not weaken today's
NO-GO evidence.

## Verification

- Red concise-brief regression failed before the fix and passed after it.
- Brief suite: 24 passed.
- Full suite: 360 passed with the two known NumPy all-NaN calibration warnings.
- Structural-stop experiment reproduced identical decision metrics on a second
  run.
- `compileall`: passed.
- `pip check`: no broken requirements.
- Doctor: `status=ok`.
- `git diff --check`: passed with CRLF normalization notices only.
- Targeted secret scan: no embedded credential values.
- Canonical market-hours reports, journal, tuning, brief, and Telegram were not
  regenerated after hours.

## Next run

Keep the daily corrected-semantic outcome cadence without lowering any gate.
The daily three-bar retest and daily-close cost-basis invalidation rules are now
closed, along with prior threshold, target, horizon, post-selection regime, and
detector-gate variants. The next legitimate strategy lever must be a genuinely
stateful intraday/multi-timeframe entry/invalidation model with its own
pre-registration and same-day drift control; do not slice either failed daily
rule into a favorable subcase.

Current run time: 2026-08-19T18:12:39-05:00.
