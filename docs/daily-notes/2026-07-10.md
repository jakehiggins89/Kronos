# Daily Kronos adjustment - 2026-07-10

## Verdict

Not live-ready (audit honestly `blocked`) — unchanged. Health check after 7 unattended days: system is clean; one data edge case found and fixed.

## Health check (first session since 2026-07-03)

- Scheduled research_ops ran every market day (7/6–7/10; 7/4–7/5 weekend). Zero errors in the logs across the week.
- 264 tests green before today's change, doctor `ok`, journal integrity 622 records / 0 duplicates, outcome review resolving normally (12 counterfactual today, 0 quarantined, 0 contract-blocked).
- Telegram is now live: credentials filled in since 7/3, daily briefs delivering (`"telegram": {"status": "sent"}`). The last remaining manual item from 7/3 is closed.
- Calibration frontier: all three objectives evaluated nightly, nothing shipped (correct — `shipped: 0` on all 40 registry rows). The 7/3 `tail_prob` lead **regressed**: tercile-spread CI lower bound is no longer positive on 7/10. The two-touch gate did its job; take-all-bullish remains policy.
- HIMS 2026-03-09 (+50% gap) and BBAI 2025-08-12 (−33% gap) split-snap warnings verified as real market moves — Tradier cross-check agrees with Alpaca on both (status ok, sub-1pp divergence). Warn-only noise by design; no action.

## Edge case found & fixed: halted sessions read as corrupt bars

- CLSK was skipped from the evidence index on **every** run since the bar-contract v2 wave: `1 zero-volume flat bars (forward-filled non-trading sessions)` at 2024-11-08.
- Root cause verified against three vendors + SEC/Nasdaq record: CLSK common stock was **halted all day 2024-11-08** (GRIID warrant clerical error; halt lifted 11/11). Alpaca and Yahoo both deliver the halted session as a forward-filled zero-volume placeholder bar; Tradier omits the session. The data is correct — the contract just had no concept of a real full-day halt, so the ticker was excluded until the date left the 2y lookback (~Nov 2026).
- Fix: `drop_vendor_placeholder_bars()` in `market_data.py` — zero-volume **flat** bars (no prints = no market information, fabricated High/Low path) are dropped before the contract check, converting the halt into a missing session governed by the existing session-completeness gate (≤5 warn, >5 fail). Zero-volume bars **with** range remain hard violations (internally impossible = genuine corruption). Not a gate weakening — the post-drop representation is exactly Tradier's.
- Same normalization wired into `outcome_reviewer` intraday fetch: a halt inside a resolution window previously meant "retry later" forever (a historical halt never gets cleaner), silently blocking the record until expiry.
- Verified live: index rebuild now indexes all 55 tickers with `errors: {}`, records 10,860 → 11,026, CLSK logged as `EDGE_INDEX_PLACEHOLDER_BARS_DROPPED` + session-completeness warning `1 expected sessions missing: 2024-11-08`, cross-source ok. 269 tests green (5 new), doctor ok. No linter is configured in this repo.

## Backtest sprint (afternoon, orchestrated: 5 worker agents + adversary, ~55 min)

Full write-up: `scanner/research/experiments/20260710_sprint/SPRINT_REPORT.md`. Audit correction: the files contain 48 serialized verdicts and exact declared cell-set coverage (47 nondegenerate after excluding the intercept-only Kronos cell), but advance registration and an exact historical rerun are not proven because preregistrations/results were co-committed and the raw input was neither snapshotted nor hashed. **0/48 serialized verdicts pass; nothing ships; the original adversary `HOLDS` verdict is withdrawn.**

What actually matters from it:

1. **Cost sensitivity needs a real model**: flat subtraction changes take-all bullish avg R +0.108 → +0.0005 at 25bps/side and negative at 50bps, but this is not a bar-level slippage model and cannot establish that “the edge dies at 25bps.”
2. **Power scenario, not a claim embargo**: 2026-11-06 is when the planned n=195 for a +10pp effect is reached only if the baseline is fixed and ~12 independent resolutions/week arrive; +5pp needs n=780. Journal WR 44.8%, Wilson CI [28%, 62%].
3. **Loss-function iteration is closed**: Huber/quantile/winsorized/rank-R all land where expected_r did; three objective families have now failed on the same 10-feature linear model. The constraint is information, not optimization.
4. **Kronos forecast values were never in the saved index**: three forecast fields have 0 finite values; `kronos_sample_count` is present but always zero. `build_edge_records_from_bars` never passes `kronos=`.
5. Secondary: geometry/compression features are objective-conditional noise (drag tail_prob, help p_win); tail_prob's tail capture (~62% of ≥2R in top tercile) is real but trades away win rate; bearish still ranks nothing (sprint bearish take-all n=4,728, avg R −0.0735).
6. Adversary's production finding: the 9-day purge can be ~1 calendar day short across holiday long weekends — can only inflate apparent skill (irrelevant to these nulls; W2's purge-14 cell bounds it at ΔIC 0.0046), but bump to 11 as a protocol-version change at the next frontier revision.

## Evening autonomous pass - purge protocol hardening

The adversary's purge-window finding is now fixed in production code rather than left as a future note. `EDGE_EMBARGO_DAYS`, `EDGE_CROSS_TICKER_EMBARGO_DAYS`, and the meta-model `META_PURGE_DAYS` now all resolve to 11 calendar days. Validation reports also record `min_required_days: 11`, so a future accidental rollback is visible in the evidence artifacts.

Why: a 5-trading-bar outcome can span roughly 10 calendar days across holiday-extended long weekends. A 9-day calendar purge could therefore let a validation candidate train against an analog whose outcome path was not fully resolved before the query bar. This only inflates apparent skill; tightening the purge is a fail-closed protocol change.

Regression coverage added:

- Same-ticker default embargo excludes a 10-calendar-day candidate and allows the 11-calendar-day candidate.
- Cross-ticker default embargo does the same for same-week market leakage.
- Calibration defaults are locked to the production edge embargo, and the default walk-forward report now shows `purge_days: 11`.

Post-patch verification:

- Focused retrieval/calibration/edge unit slice: 37 passed.
- After a concurrent stats p-value change appeared, focused stats slice: 10 passed.
- Final current-tree full test suite: 276 passed, with only the known all-NaN calibration warnings. One prior full-suite run hit a transient Kronos `MSE nan`; the isolated Kronos case passed on rerun before the final full-suite pass.
- `compileall`, `pip check`, doctor, and `git diff --check` all passed; diff check emitted CRLF normalization warnings only.

Post-patch `research_ops` at 2026-07-10 17:34-17:40 CT:

- Completed in 400.782 seconds; journal integrity 661 records / 0 duplicates.
- Outcome review checked 10 pending records and resolved 0; no quarantine or bar-contract block.
- Research-only candidates taken for counterfactual tracking: `LUNR` bearish score 78 and `IONQ` bearish score 73. Both failed Kronos direction agreement and remain research-only, not live alerts.
- Edge scan: 30 tickers, 11 active rejects, 19 skips, 0 research/promoted live candidates. Top active rejects were `SOFI` bullish edge 18.03, `F` bullish edge 11.64, and `RIOT` bearish edge 5.43.
- Reject mix: `setup_gate_failed` 11, `options_data_not_execution_grade` 11, `edge_score_below_research_threshold` 11, `non_positive_analog_expectancy` 5, `wide_options_spread` 3.
- Validation now uses purge 11 for edge analogs and all three calibration objectives. Current meta-model ICs still fail: expected_r -0.0900, tail_prob -0.0661, p_win -0.0148; nothing shipped. Current code records `day_cluster_method: entry_day_cluster_hac_lag5_cr1`; the quoted ICs were produced by the earlier one-way method and are retained as historical run output.
- Audit remains `blocked`: `validation_threshold_55_unsupported` and `ranking_evidence_unsupported`. Ranking IC -0.0181, p 0.757808, top-decile average R -0.0183, bullish within-direction IC -0.0390, bearish IC -0.0171.

Verdict remains NO-GO. The software is healthier, but the live blockers are evidence quality and after-cost/ranking support. Because this run was after market close, options execution-grade warnings are expected to be noisier; the scheduled market-hours run remains the better options-liquidity evidence source.

## Late automation pass - journal dedupe regression fix

The 17:34-17:40 CT `research_ops` result above was directionally useful but its journal integrity line was wrong. A follow-up inspection of the taken/rejected trade journal found repeated same-day research entries: `LUNR` appeared twice and `IONQ` appeared three times as pending bearish research candidates on 2026-07-10. Root cause: `decision_fingerprint()` still included moving `entry_price`, so the same setup/day became a new sample whenever the quote moved.

Fix shipped in `scanner/learning/outcome_store.py`:

- Removed `entry_price` from the setup/day fingerprint so repeated same-day scans merge instead of inflating pending/resolved counts.
- Restored enrichment merging in `deduplicate_decisions()` so cleanup keeps diagnostics from duplicate rows while removing the extra sample.
- Expanded `scanner/tests/test_outcome_store.py` so duplicates with changed entry prices are covered.

Corrected `research_ops` at 2026-07-10 18:05-18:11 CT:

- Journal cleanup: 664 input rows -> 603 unique rows; 61 duplicate samples removed. Post-refresh dedupe check is now 603/603, 0 duplicates, with no repeated same-day pending groups.
- Outcome review checked 8 pending records and resolved 0; no quarantine or bar-contract block.
- Research-only candidates taken for counterfactual tracking remained `LUNR` bearish score 78 and `IONQ` bearish score 73. They are research-only, not live alerts.
- Edge scan remained 30 tickers: 11 active rejects, 19 skips, 0 research/promoted live candidates. Top active rejects remain `SOFI` bullish edge 18.03, `F` bullish edge 11.64, and `RIOT` bearish edge 5.43.
- Reject mix unchanged: `setup_gate_failed` 11, `options_data_not_execution_grade` 11, `edge_score_below_research_threshold` 11, `non_positive_analog_expectancy` 5, `wide_options_spread` 3.
- Corrected learning state: 30 resolved research candidates, 12 wins, 18 losses, 40.0% win rate, average 5-bar return -0.2426%; 8 pending. Adaptive policy remains `hold_no_edge`; no threshold loosening.
- Audit remains `blocked`: `validation_threshold_55_unsupported` and `ranking_evidence_unsupported`. Ranking IC -0.0181, p 0.757808, top-decile average R -0.0183; bullish within-direction IC -0.0390, bearish IC -0.0171.

Verification after the dedupe fix: focused outcome-store test passed (4), focused learning/research slice passed (22), full suite passed (278, only known all-NaN calibration warnings), `compileall` passed, `pip check` passed, doctor `ok`, and `git diff --check` passed with CRLF normalization warnings only.

Verdict remains NO-GO. The key change from this late pass is that the learning evidence is now less inflated and more honest.
