# Daily Kronos adjustment - 2026-08-17

## Verdict

NO-GO / `blocked`. Keep live trading, live alerts, and paper promotion disabled.

Today's source-fingerprinted market-hours evidence is complete and internally consistent, but it is negative rather than merely immature. Both readiness routes have more than their required independent-day coverage and still lose after the pre-registered 25 bps/side cost floor.

## Scheduled run and learning journal

The 13:30 CT primary launch was interrupted during the first research-scan ticker. Task Scheduler's `IgnoreNew` policy prevented overlap, and the 14:00 recovery completed the cycle in 390.766 seconds with evidence run ID `20260817T190257Z-87226ca5`. The 14:30 recovery correctly returned `skipped_already_complete`. Task Scheduler reports exit `0x00000000`, zero missed runs, and the next launch at 13:30 CT on Tuesday, 2026-08-18.

The first launch resolved five August 7/10 counterfactuals before it stopped: `LYFT` bullish +0.2895R, `CHPT` bullish +0.2118R, `PLTR` bullish +0.3180R, `LUNR` bullish -1R stop, and `TTD` bearish -1R stop. The journal now has 76 resolved research candidates (34W/42L, 44.7% raw win rate) and two pending. Adaptive policy held the research threshold at 65 with `hold_no_edge`; no tuning override changed.

Today's research scan accepted one counterfactual only: `RIVN` bearish score 73, Doctrine v2 65 fail, Kronos agreement 30% fail. It was not alerted or traded.

## Accepted and rejected Edge rows

The Edge scan produced 19 skips, ten rejects, one research recommendation, zero promotions, zero paper-authorized candidates, and zero live trades.

- `AAL` bearish was the only Edge research row: score 50.61, seven analogs, +0.0415 net average analog R versus +0.1008 gross. Fresh Tradier OPRA liquidity passed, but the row remains below the 65 promotion score and the entire bearish direction is blocked.
- `GME` bearish scored 40.51 with -0.1646 net analog R; rejecting it for non-positive expectancy and sub-45 score is correct.
- `RIVN` bearish scored 9.94 in Edge despite the separate research-scan acceptance because its live Edge setup gate failed. The two stages serve different purposes; no override is warranted.
- The other active rejects all failed setup and the Edge research threshold. Five active rows had non-positive net analog expectancy. `KEY` also had no liquid Tradier contract. No rejected row merits promotion or a threshold exception.

## Evidence result: mature and negative

The corrected-score index contains 11,029 records and validation used the latest 1,500 purged walk-forward candidates. Scan and validation fingerprints matched each other and the runtime that generated them.

- Score 55: 58 raw signals / 42 independent entry days (minimum 20), -0.2621R per day, HAC t=-2.8519, dependence-aware precision lower bound 0.2084 versus the required 0.55.
- Ranking: IC +0.0171 with clustered p=0.303333, below the required +0.07 / p<=0.05.
- Top decile: 150 raw signals / 56 independent days (minimum 20), -0.1650R per day, HAC t=-2.0905, precision lower bound 0.2691 versus 0.45.
- Bullish: 65 days, -0.0470R per day, t=-0.6836. Bearish: 65 days, -0.2149R per day, t=-2.3239. Both directions remain blocked.

The current strategy is therefore not waiting only on sample count. The tested entry/ranking design is negative at adequate independent-day coverage. Prior pre-registered horizon and regime experiments also found negative selection value versus matched random-entry placebos, so the next legitimate strategy change must be a new pre-registered entry-selection hypothesis rather than another threshold, target, horizon, or post-selection regime adjustment.

## Mature-negative brief diagnosis

The daily brief contained a concrete interpretation bug: it described both failed gates as "not yet," claimed score 55 had no supporting signals, and told the operator to do nothing except wait for more resolved samples. That is false for today's 42-day score-55 and 56-day top-decile cohorts and hides a losing tested mechanism as ordinary evidence immaturity.

`scanner/brief.py` now distinguishes `COLLECTING`, `UNSUPPORTED`, and `NEGATIVE` gate states. It marks a gate negative only when dependence-aware metrics are present, the independent-day minimum is met, and both average R and its t-stat are non-positive. When both readiness routes are mature-negative, the brief says no operator action is required but strategy evidence is negative, keeps live off, and directs the autonomous next step toward a pre-registered entry-selection redesign. Static issue text no longer claims score 55 has no signals. `scanner/README.md` documents the behavior, and `scanner/tests/test_brief.py` carries the regression.

I did not rerun the full lab after hours or overwrite the canonical market-hours artifacts. The reporting source change intentionally changes the runtime fingerprint, so live preflight now fails closed until Tuesday's scheduled market-hours lab stamps the new source. That is safer than replacing fresh OPRA evidence with a stale after-hours chain.

## Verification

- Red regression reproduced the misleading "Nothing to do" / more-samples diagnosis before the fix.
- Focused brief suite: 24 passed.
- Full suite: 358 passed with the two known NumPy all-NaN calibration warnings.
- `compileall`: passed.
- `pip check`: no broken requirements.
- Doctor: `status=ok`.
- In-memory brief against today's evidence reports both gates `NEGATIVE` and recommends a pre-registered entry-selection hypothesis.
- `git diff --check`: passed with CRLF normalization notices only.
- The three pre-existing untracked 2026-08-13 experiment directories remain untouched.

## Next run

Tuesday's scheduled cycle should create the first canonical report carrying the mature-negative brief semantics and the new runtime fingerprint. Confirm the 13:30 primary launch or recovery completes, preserve the gross/net analog provenance, and keep live/paper paths disabled. The next research lever is entry selection itself; do not loosen thresholds or reopen the already falsified horizon, target, and post-selection regime variants.

Current run time: 2026-08-17T18:12:56-05:00.
