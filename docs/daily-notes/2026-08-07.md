# Daily Kronos adjustment - 2026-08-07

## Verdict

NO-GO / `blocked`. Live trading, live alerts, and paper promotion remain disabled.

Today's market-hours cycle completed normally and produced no live or Edge-qualified trade. It did accept four bullish research-only counterfactuals for future outcome review. The Edge rejects were defensible, and current validation still shows no rankable out-of-sample edge. Code review found an operator-reporting blind spot: the daily brief said "TRADES - none" and "0 qualified" without disclosing the four accepted research samples. The brief now separates live/Edge-qualified trades from counterfactual research evidence.

## Scheduled market-hours cycle

- `Kronos Daily Research Ops` ran from 13:30:03 to 13:36:59 CT and completed in 416.328 seconds. The 14:30 recovery launch correctly returned `skipped_already_complete`; Task Scheduler reports `0x00000000`, zero missed runs, and the next run at 13:30 CT on Monday, 2026-08-10.
- Journal integrity was 1,093 input / 1,093 unique / zero duplicates before today's scan.
- Outcome review checked five pending rows and resolved zero. No live or counterfactual outcome had a complete new window today.
- Research scan checked 30 tickers: four passes, 26 skips, and zero errors.
- Edge evidence run `20260807T183300Z-ed4d157a` has matching scan/validation provenance and fresh intrinsic completion timestamps.

## Accepted research-only evidence

Four counterfactual samples were accepted for outcome tracking. None was alerted, traded, or Edge-qualified:

- `LYFT` bullish, research score 68: Doctrine v2 80 passed; Kronos agreement 0.00 failed.
- `CHPT` bullish, research score 73: Doctrine v2 65 failed; Kronos agreement 0.40 failed.
- `LUNR` bullish, research score 83: Doctrine v2 60 failed; Kronos agreement 0.10 failed.
- `PLTR` bullish, research score 68: Doctrine v2 80 passed; Kronos agreement 0.00 failed.

All four therefore remain useful disagreement experiments, not trade candidates. The journal now has 66 resolved research candidates (29 wins / 37 losses raw) and nine pending. The score-65 cohort has 30 independent rows across 16 entry days, but its dependence-adjusted lower bound is only 0.4113 and return HAC t-stat is 1.0647. Bullish score-65 evidence is especially weak at nine rows / eight days, -1.9488% average daily return, and HAC t=-2.5029. The adaptive policy correctly held the global research threshold at 65; research collection remains counterfactual and no live gate was loosened.

## Rejected trades audited

The canonical Edge scan had 17 skips, 13 active rejects, zero Edge research candidates, and zero promotions. Every active row failed both Potter and Empty Space setup gates and remained below the Edge research threshold:

- `PFE` bullish led at Edge 25.03. Its seven analogs were 5W/2L and averaged +0.4552R, but the current setup gates failed and the score was far below 45.
- `IONQ` bullish scored 11.02. Its seven analogs were 4W/3L and averaged +0.3615R, but both current setup gates failed.
- `SOUN` bullish scored 9.38. Its seven analogs were 5W/2L but averaged only +0.0442R; both current setup gates failed.
- `GME` bearish had mildly positive analog expectancy (+0.0788R) but scored zero, failed both setup gates, and bearish promotion is independently blocked by validation.
- The other nine active rows had non-positive analog expectancy. `LYFT` and `CHPT` also had no liquid options contract that cleared the chain filters.

Rejection counts were `setup_gate_failed` 13, `edge_score_below_research_threshold` 13, `non_positive_analog_expectancy` 9, and `options_no_liquid_contract` 2. Tradier OPRA evidence was fresh; current Edge equity bars were free Alpaca IEX (`feed_confidence=0.7`), which remains research-grade rather than capital-grade. No reject should be promoted or manually overridden.

## Go-live evidence

Readiness remains blocked on `validation_threshold_55_unsupported` and `ranking_evidence_unsupported`:

- threshold 55 has one evidence day versus 20 required;
- pooled rank IC is -0.0123 with entry-day-clustered p=0.672020;
- the top decile has 150 raw rows across 59 evidence days, -0.0183 mean daily R, HAC t=-0.1810, and dependence-aware precision lower bound 0.3936;
- bullish validation has 64 days and +0.0540 mean daily R but is unproven at HAC t=0.7470;
- bearish validation has 64 days and -0.1630 mean daily R and remains blocked at HAC t=-1.6258.

More sample count has not produced stronger ranking evidence. There is no promotable direction, no current Edge candidate, and no basis for go-live.

## Daily-brief evidence fix

The scheduled Telegram brief reported zero qualified trades even though `research_scan` had accepted four pending counterfactuals. That conflated two intentionally different paths and hid the evidence that daily review is supposed to inspect.

Updated `scanner/brief.py`, `scanner/tests/test_brief.py`, and `scanner/README.md`:

- the brief reads today's accepted research candidates from the journal without loading models or calling the network;
- live/Edge-qualified trades now have an explicit `LIVE TRADES` label;
- accepted samples appear in a separate `RESEARCH SAMPLES - counterfactual only` block with research score, Doctrine v2 result, and Kronos agreement;
- the markdown brief states that the samples were never alerted or traded;
- malformed journal lines cannot take down the operator brief; journal-integrity remains responsible for repair.

The regression failed before the change because the two accepted fixtures still rendered as zero qualified with no research-sample section. It passes after the fix. The canonical `daily_brief.md` was regenerated from today's existing evidence with Telegram explicitly disabled, so no duplicate status message was sent and no market scan was rerun.

## Verification

- Focused brief tests: 17 passed.
- Full suite: 334 passed with the two known NumPy all-NaN calibration warnings.
- Full-suite runtime isolation: zero files created, removed, or changed under `scanner/reports` and `scanner/tuning`.
- `compileall`: passed.
- `pip check`: no broken requirements.
- Doctor: `status=ok`.
- `git diff --check`: passed with CRLF normalization notices only.
- Tracked-diff secret-assignment scan: zero matches.

## Next run

Do not replace Friday's evidence with a weekend duplicate scan. Keep Monday's 13:30 CT cycle, continue resolving only complete counterfactual outcomes, and watch the new all-bullish disagreement cohort without optimizing to four correlated same-day samples. Keep every execution and evidence gate unchanged until ranking, direction, and capital-grade data requirements are all genuinely supported.
